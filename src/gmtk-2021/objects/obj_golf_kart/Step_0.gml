/// @description Directional logic, move to goal

var dir_goal = point_direction(x, y, goal[0], goal[1]);

// var dir_vector = 


/*
speed_range = [5, 8]
goal = [room_width / 2, room_height / 2];
vector_speed = [0, 0];
current_direction = point_direction(x, y, goal[0], goal[1]);

acceleration = 0.1;
